import openai
import lmql
import asyncio
import nest_asyncio

nest_asyncio.apply()

from lmql.lib.chat import message

@lmql.query()
def chatbot():
    '''lmql
    print("Chatbot is ready. Type '^d' to exit.")
    argmax 
        while True:
            "{await input()}"
            "[@message ANSWER]"
            print(ANSWER)
    from "chatgpt"
    '''

result = chatbot()
print(result.variables["ANSWER"])